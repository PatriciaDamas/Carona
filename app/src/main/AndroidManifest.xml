<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carona">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/carona"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppHome">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppHome" />
        <activity
            android:name=".CriarViagemActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppHome" />
        <activity
            android:name=".ProcurarViagemActivity"
            android:label="CARONA"
            android:theme="@style/AppTab" />
        <activity
            android:name=".PerfilActivity"
            android:label="CARONA"
            android:theme="@style/AppTab" />
        <activity
            android:name=".DefActivity"
            android:label="CARONA"
            android:theme="@style/AppTab" />
        <activity android:name=".InfoViagemActivity"></activity>
    </application>

</manifest>